# GEMINI.md

Agent guidance for this repo lives in **[`CLAUDE.md`](./CLAUDE.md)** — a single
source of truth so every agent follows the same rules. See it for architecture,
the verification loop (`lint` / `typecheck` / `test` / `build-storybook`),
dependency rules, and conventions.
